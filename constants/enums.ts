export enum BOX_STATUS {
    Created = 'Created',
    Collecting = 'Collecting',
    Collected = 'Collected',
}

export enum PACKING_TYPES {
    STANDARD = "STANDARD", // Oddiy
    OVERSIZED = "OVERSIZED", // Gabarit
}